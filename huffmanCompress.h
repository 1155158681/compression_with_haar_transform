/*
	HuffmanCompress
	The file contains algorithm for encoding binary data
	via Huffman Coding process.
*/

#ifndef HUFFMANCOMPRESS_H
#define HUFFMANCOMPRESS_H


class HuffmanTree {
	public:
		
	private:
		
};
void huffmanCompress(char const* fileName, int headerBuffer = 0);
void huffmanDecompress(char const* fileName, int headerBuffer = 0);

#endif
